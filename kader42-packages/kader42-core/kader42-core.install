post_install() {
    # Hardware-Datenbank aktualisieren
    systemd-hwdb update
    udevadm trigger 2>/dev/null || true

    # Software-Center Sync ausführen
    if [ -x /usr/bin/kader42-discover-sync ]; then
        /usr/bin/kader42-discover-sync
    fi
}

post_upgrade() {
    post_install
}